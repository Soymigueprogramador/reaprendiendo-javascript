/*
    Datos primitivos
Tipos de datos: 
Let números = 1; 
Cuando el valor de una variable es un number no se tienen que usar las comillas.
Let texto = ‘este es un texto’;
Cuando el valor de una variable es texto o string si se tienen que usar las comillas. 
Let verdadero = true;
Let falso = false; let null = null; 
Estos son datos booleanos, cuando escribimos algunos de estos dos valores tenemos que hacerlo sin usar las comillas y este tipo de datos solo nos devolverá un true o folse. 
Let noDefinido = undefined;
Let noEstoySeguro = ‘’;
Cuando tenemos una variable que no tienen asignado un valor se lo conoce como undefined, este es un tipo de valor que se aplica cuando las variables no tienen valore asignado. 
Let null = null; 
Esto ocurre cuando tenemos una variable que no queremos que sea undefined, es decir que nunca le asignamos un valor, pero tampoco queremos que esta variable tenga un valor determinado si no que su valor sea nulo. 
Esto lo podemos usar cuando tenemos un menú desplegable, pero queremos que el usuario decida si le agrega un valor o no. 

*/

// Aplicando les ejemplos de los datos primitivos. 
let numero = 1; //Su valor es un numero.
let texto = 'miguel'; // Su valor es un texto, o sea, un string.
let verdadero = true; // Es un valor booleano
let falso = false; //Es tambien un valor booleano
let noDefinido  = undefined; //No tiene asignado ningun valor.
let noEstoySeguro = null; //Se utiliza cuando no quiero asignar un valor espesifico pero tampoco quiero dejarlo como undefined. 
 
console.log(numero);
 console.log(texto);
 console.log(verdadero);
 console.log(falso);
 console.log(noDefinido);
 console.log(noEstoySeguro);