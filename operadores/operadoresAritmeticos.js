/*
    Operadores aritméticos
¿para que se usan los operadores aritméticos en JavaScript?
Los operadores aritméticos de JavaScript se usan para realizar operaciones matemáticas.
Ejemplo:
Let a = 5;
Let b = 7;
Console.log(a + b); este operador pertenece al operador de adición.
Console.log(a - b); este operador pertenece al operador de resta. 
Console.log(a * b); este operador pertenece al operador de multiplicación. 
Console.log(a / b); este operador pertenece al operador de división. 
Console.log(a % b); este operador pertenece al operador de modulo, 
     se queda con la diferencia de una división.
Console.log(a ** b); este operador pertenece al operador de potencia, 
        toma el valor de la primera variable y lo eleva al valor de la segunda variable 
console.log(s ++); este operador pertenece al operador de incrementar, con el primer signo de  más este toma el valor de la variable y lo incrementa en 1 y el segundo signo de mas lo muestra en la consola.
Cosole.log(a --); este operador pertenece al operador de decrementar, con el primer signo de  menos este toma el valor de la variable y lo decrementa en 1 y el segundo signo de menos lo muestra en la consola.

*/

let a = 5;
let b = 7;

console.log(a + b);
console.log(a - b);
console.log(a * b);
console.log(a / b);
console.log(a % b);
console.log(a ** b);
console.log(a ++ );
console.log(a -- );